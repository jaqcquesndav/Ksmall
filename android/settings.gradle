// Import Expo-specific settings
apply from: new File(["node", "--print", "require.resolve('@expo/metro-config/package.json')"].execute(null, rootDir).text.trim(), "../build/android-settings.gradle");

// Original project settings
rootProject.name = 'ksmall'
include ':app'

// Additional repositories for React Native dependencies
apply from: new File(["node", "--print", "require.resolve('expo/package.json')"].execute(null, rootDir).text.trim(), "../scripts/autolinking.gradle");
apply from: new File(["node", "--print", "require.resolve('react-native/package.json')"].execute(null, rootDir).text.trim(), "../react.gradle");
